# Architecture frontend (web/)

Application React 19 + TypeScript construite par Vite, stylée avec Tailwind 4, état géré par Zustand. Elle tourne dans la WebView2 unique de l'hôte et porte toute l'interface : arborescence des workspaces, onglets, splits, terminaux xterm.js, Leader et palette.

## Structure

| Dossier | Rôle |
| --- | --- |
| `src/model/` | Modèle de session pur (types, fabriques, helpers d'arbre `panesOf`, `replaceNode` (rend le même nœud quand rien ne change dans un sous-arbre), `pruneNode`, `splitLeaf`, `setRatioAt` (chemin `SplitPath` de côtés `SplitSide`, ratio borné par `clampRatio`), sélecteurs `activeWorkspace`, `activeTab`, `activePane` ; un onglet porte `explorer` (panneau de droite ouvert) et `panel` (`RightPanelView` : Fichiers ou Git, Fichiers si absent), la session `explorerWidth` (largeur du panneau de droite) et `gitGraph` (`GitGraphLayout` : largeur et ouverture de la colonne des branches, largeurs des colonnes du graphe, affichage d'Auteur et de Date, bornés par `clampGitGraph`)). Aucune dépendance React ni DOM. |
| `src/store/` | Stores Zustand : `sessionStore` (session et toutes ses mutations, écrites sur un brouillon Immer : seuls les objets modifiés changent d'identité et une mutation sans effet rend la même session), `hostStore` (connexion, shells, message de statut, état Leader, liste des projets et contextes Git par pane reçus de l'hôte, réglages de persistance reçus dans `app.hello` ou `settings.result`, instantané des réglages pour l'écran Paramètres, indicateur `unsaved` levé par `session.saveFailed`, effacé par `session.saved`), `paneStore` (état local d'un pane : shell qui a échoué au démarrage, shell terminé ou dossier disparu avec son repli ; un dossier disparu ignoré par l'utilisateur n'est plus signalé pour ce pane ; jamais persisté), `agentStore` (états d'agents par pane reçus dans `agent.states`, remplacés intégralement à chaque message ; `acknowledge(paneId)` mémorise l'état-message rejoint ou ignoré pour masquer la carte de `AttentionToasts` ; cet acquittement est levé dès que l'état du pane change, si bien qu'une nouvelle attente identique après un passage « en cours » notifie à nouveau jusqu'au prochain changement d'état de ce pane ; jamais persisté), `gitStore` (vue Git, jamais persisté : dossier suivi `path` et dernier dossier résolu par l'hôte, état du dépôt, graphe affiché `graphOpen` (rouvert à chaque ouverture de la vue Git), historique chargé avec sa portée et le nombre de commits demandés, commit à amener dans le graphe `reveal`, ligne choisie `commit` (nulle pour la ligne WIP) avec son détail, fichier du volet de diff `GitFileTarget` avec son diff, chacun avec son numéro de requête pour ignorer les réponses dépassées, opération en cours `busy`, échec et refus de push à afficher, confirmation `GitConfirmation` et saisie en place `GitPrompt` en attente, message de commit et case « Amend du dernier commit », glisser-déposer en cours `GitDrag` et menu contextuel du graphe `GitMenuRequest` ; un changement de dépôt remet tout à zéro), `explorerStore` (explorateur de fichiers, jamais persisté : contenu des dossiers reçu dans `files.listed` indexé par chemin, dossiers dépliés, élément sélectionné, renommage ou création en cours (`EntryDraft`) et suppression à confirmer (`DeleteRequest`) ; `moved` et `removed` suivent la sélection après un renommage ou une suppression), `uiStore` (état d'édition transitoire, comme le workspace ou l'onglet en cours de renommage et l'origine de son éditeur (`RenameOrigin` : en-tête, panneau ou barre d'onglets), l'ouverture de la palette, du sélecteur de projets ou des Paramètres, mutuellement exclusifs, les workspaces ouverts pendant un glisser et la confirmation de fermeture en attente `closeConfirmation`, jamais persisté). |
| `src/agents/` | `agentSummary.ts` (libellés français des agents et des états, `describeAgent`, `waitingPanes(session, agents)` liste les panes en attente avec leur libellé « workspace › onglet › dossier », `tabAgents` (état dominant d'un onglet et infobulle), `workspaceStateCounts` (nombre de panes par état), `workspaceSummary` (pastilles d'attente et d'erreur, glyphe d'activité), `stateBreakdown` (répartition « En attente : 1 · En cours : 2 »), `nextPaneInState` (pane suivant dans un état, en boucle à partir du pane actif), `alertStateCounts` (attentes et erreurs cumulées d'un ensemble de workspaces) ; aucune dépendance React), `attentionNotifier.ts` (`startAttentionNotifier` observe `agentStore` et envoie `attention.raise` à l'hôte pour chaque pane qui entre dans une attente nouvelle et non acquittée, une seule fois par état-message ; l'hôte décide ensuite selon l'activation de la fenêtre et les réglages). |
| `src/explorer/` | `fileTree.ts` (`buildFileTree` aplatit l'arbre visible à partir du dossier racine, des contenus reçus et des dossiers dépliés en lignes `TreeRow` (élément, saisie d'un nouveau nom, note « Chargement… », erreur ou « … et N autres éléments non affichés ») et rend la liste des dossiers à surveiller : la racine et chaque dossier déplié visible ; `targetFolder` choisit où créer : dossier sélectionné, dossier parent d'un fichier sélectionné, sinon racine), `fileExplorerActions.ts` (`watchFolders` n'envoie `files.watch` que si la liste change, ouverture, création, renommage, suppression confirmée, copie du chemin, réception des messages `files.*` ; après une création ou un renommage le focus rejoint la ligne concernée si personne d'autre ne l'a pris). Aucune dépendance React. |
| `src/git/` | `gitRequests.ts` (suivi du dépôt par `git.watch` seulement quand le dossier change, historique par pages et portée, choix de la ligne WIP ou d'un commit avec lecture de son détail (différée de 120 ms au clavier, relue au prochain état du dépôt si elle a échoué), `revealCommit` choisit un commit et demande des pages jusqu'à le trouver dans le graphe (au plus 10 000 commits, sinon message dans la barre de statut), ouverture d'un diff avec un numéro de requête, stage, unstage, abandon après confirmation, commit, push, pull, fetch, push forcé après confirmation, annuler, conflits : ouvrir dans l'éditeur via `files.open`, marquer résolu, terminer, abandonner après confirmation ; `retryOnFailure` / `takeRetry` transforment un échec codé (`notMerged`, `conflictMarkers`) en confirmation puis en nouvelle demande confirmée), `gitRefActions.ts` (merge, rebase, cherry-pick, checkout d'un commit, reset avec confirmation du reset hard, checkout d'une branche locale ou distante, saisies en place d'une branche, d'un renommage, d'un tag ou d'un message de stash puis `submitPrompt`, suppression de branche (confirmation si elle est sans merge) ou de branche distante (toujours confirmée), tags, stash), `gitGraphActions.ts` (menus d'un commit ou d'un stash, de la ligne WIP, d'une étiquette et des colonnes du graphe, double-clic sur une étiquette qui fait le checkout de sa branche ; le menu rend le focus au graphe en se fermant), `gitDrag.ts` (glisser-déposer d'une branche par événements pointeur et `elementFromPoint` sur les attributs `data-git-drop-*`, seuil de 4 px, capture du pointeur ; cible acceptée seulement si l'une des deux branches est la branche courante et qu'aucune opération n'est en cours ; le dépôt ouvre le menu Merge / Rebase), `gitReceivers.ts` (réception des messages `git.*` : un état ou un `git.changed` recharge le diff du fichier affiché, le volet suit un fichier passé d'un groupe à l'autre et se ferme s'il a disparu, l'apparition de conflits revient sur la ligne WIP, un historique reçu poursuit la recherche d'un commit demandé, `git.done` / `git.failed` / `git.pushRejected` mettent fin à `busy` et annoncent le résultat dans la barre de statut), `gitMenus.ts` (entrées des menus d'une branche, d'une branche distante, d'un tag, d'un stash, d'un commit, de la ligne WIP, d'une étiquette (menu de la branche locale suivi de la suppression de ses branches distantes réunies), du dépôt d'un glisser (`canDropRef`, `dropMenu`) et des colonnes du graphe), `gitRows.ts` (lignes des modifications, conflits puis Staged puis Unstaged), `gitLabels.ts` (libellés des statuts, conflits et opérations, dates relatives et `jj/mm/aaaa hh:mm`, initiales d'un auteur, texte et infobulle d'une étiquette, nombre de fichiers de la ligne WIP par nature, chemin absolu d'un fichier du dépôt), `gitFocus.ts` (focus dans le panneau Git ou dans le graphe, `requestGraphFocus` : le graphe prend le focus dès qu'il s'affiche, retour du focus quand une ligne disparaît). Aucune dépendance React. |
| `src/panel/` | `rightPanel.ts` : `togglePanelView(vue, focus)` ouvre le panneau sur la vue demandée (Fichiers ou Git), bascule de l'une à l'autre ou ferme le panneau si cette vue était déjà affichée, donne le focus à la vue (pour Git, au graphe, rouvert à chaque ouverture) ou le rend au terminal ; `toggleRightPanel` (bouton de la barre d'onglets et croix du panneau) ouvre ou ferme sur la dernière vue ; `showPanelView` change de vue depuis les onglets du panneau ; `showGitGraph` / `hideGitGraph` / `toggleGitGraph` affichent le graphe avec le focus ou le masquent en rendant le focus au terminal. |
| `src/bridge/` | `messages.ts` (types des messages, miroir du contrat C#), `bridge.ts` (envoi et abonnement via `window.chrome.webview`). |
| `src/terminal/` | `terminalRegistry` (une instance xterm.js par pane, conservée hors React pour survivre aux changements d'onglet ; renderer WebGL, repli canvas puis DOM ; au plus 14 contextes WebGL, Chromium retirant le plus ancien au-delà de 16 : quand un terminal affiché en ferait dépasser le nombre, le terminal caché affiché le moins récemment rend le sien et le retrouve à son prochain affichage, jamais un terminal visible ; après une perte de contexte, le terminal visible passe en canvas et WebGL est retenté à son prochain affichage ; acquittements de flux ; `snapshot` sérialise le texte de panes donnés via `@xterm/addon-serialize` (onglet fermé), `dirtyPaneIds` liste les panes ayant reçu de la sortie depuis leur dernière sauvegarde et `takeSnapshot` en sérialise un (l'historique au-dessus de la zone visible ne change plus : il est sérialisé une seule fois en morceaux bornés par des marqueurs xterm, `cacheStableText` en ajoute au plus 2 000 lignes par appel et `takeSnapshot` n'y ajoute que ce qui reste, dont la zone visible ; le cache est vidé au redimensionnement et au changement de buffer, et le buffer alternatif est toujours sérialisé en entier), `unsavedPrimedText` renvoie le texte d'un onglet rouvert pas encore affiché, `markAllDirty` force une sauvegarde complète, `prime` réinjecte ce texte suivi d'un séparateur `RestoreKind` (onglet rouvert ou « Session restaurée — nouvelle session ») avant de lancer un nouveau processus, `configure` fixe le nombre de lignes conservées par pane ; les URL détectées par `@xterm/addon-web-links` et les hyperliens OSC 8 (`linkHandler`, tous schémas acceptés pour que l'hôte puisse signaler un refus) sont soulignés au survol et envoient `link.open` sur Ctrl + clic seulement, le clic simple reste à la sélection ; l'hôte valide le schéma), `textPersistence` (`primeSessionText` au démarrage, qui garde aussi en mémoire, hors du store, le texte des onglets fermés ; `startTextAutosave` toutes les `textIntervalSeconds` met en cache l'historique de chaque pane modifié par tranches pendant les temps morts (`requestIdleCallback`), puis envoie un `text.save` par pane avec la liste `keep` ; `keepClosedTabText` / `takeClosedTabText` conservent et rendent le texte d'un onglet fermé ; `forgetRemovedText` purge le texte des panes disparus ; `markTextSaveFailed` fait tout renvoyer à la sauvegarde suivante ; `saveTextNow` sérialise d'un coup les panes modifiés, `closeApplication` envoie la disposition (`session.save`) et ce texte puis `window.close`), `closeGuard` (garde-fou avant toute fermeture : `requestClose(titre, panes, action)` demande `terminal.activity` à l'hôte, exécute l'action directement si aucun programme ne tourne, sinon ouvre `CloseConfirmDialog` avec la liste des panes concernés et de leurs processus ; `requestApplicationClose` pour Alt + F4, `receiveApplicationClosing` pour `app.closing` reçu de l'hôte (fermeture immédiate si rien ne tourne, sinon `window.closeCancel` puis confirmation) ; `confirmClose` exécute l'action retenue, `cancelClose` abandonne), `tabLifecycle` (fermeture d'un onglet, d'un pane ou d'un workspace entier avec capture du texte confiée à `textPersistence`, toujours via `closeGuard`, réouverture du dernier onglet fermé), `contextActions` (actions sur le dossier réel du pane : copier le chemin, ouvrir dans l'éditeur ou l'explorateur via `context.open`, copier la branche après `context.query` ; `gitSummary` produit « Aucun dépôt Git », « HEAD détachée », « Aucune branche » ou « Branche : x », jamais de valeur fictive), `paneLifecycle` (relance du shell d'un pane, relance dans un dossier de repli, changement de shell puis relance, ou abandon du signalement ; `terminalRegistry.restart` renvoie `terminal.create` sur la même instance xterm.js, le texte précédent reste visible), `TerminalPane` (composant qui attache l'instance à son élément une seule fois par montage : l'effet dépend de l'identifiant du pane, pas de l'objet, pour qu'une mutation de session ne relance ni attachement ni `fit()`). |
| `src/palette/` | `searchFilter.ts` (type `SearchItem` et filtrage par mots, insensible à la casse et aux accents, partagé par la palette et le sélecteur de projets), `paletteItems.ts` : construit la liste de la palette (commandes avec leur raccourci, nouvel onglet par shell disponible, renommages qui ouvrent les éditeurs inline, déplacement de l'onglet vers un autre workspace, puis navigation vers chaque workspace, onglet et pane ; en tête, une entrée « Rejoindre · workspace › onglet › dossier » par pane en attente d'un agent) et la filtre par mots sans tenir compte des accents ni de la casse. Les commandes marquées favorites (`session.favorites`, identifiants stables des commandes) sont placées en tête de liste. Aucune dépendance React. |
| `src/keyboard/` | `shortcuts.ts` : enum `Command` et une seule fonction `runCommand`, déclenchée soit par le Leader Ctrl + Espace (5 s) puis lettre, soit par un raccourci direct Ctrl + Maj + lettre ou Alt + flèche (tableau en section 9 de la spec). Les flèches (directes ou après Leader) changent de pane de façon spatiale via `paneNavigation`. Leader puis « , » (ou l'engrenage de l'en-tête, ou l'entrée « Paramètres » de la palette) ouvre l'écran Paramètres, Ctrl + P (ou Ctrl + Maj + P) ouvre la palette, Ctrl + Maj + C/V copier/coller (Ctrl + C copie aussi quand une sélection existe et la désélectionne, sinon il interrompt le shell ; Ctrl + V colle toujours), Ctrl + Maj + PageUp/PageDown (ou Leader puis PageUp/PageDown) déplace l'onglet actif, Ctrl + Maj + Z (ou Leader puis Z) rouvre le dernier onglet fermé, Ctrl + Maj + E (ou Leader puis E) ouvre le panneau de droite sur les fichiers avec le focus dans l'arbre, ou le ferme en rendant le focus au terminal, Ctrl + Maj + G (ou Leader puis G) fait de même avec la vue Git, dont le graphe prend la place des terminaux avec le focus, Alt + F4. Tout est intercepté dans xterm.js via `attachCustomKeyEventHandler`, jamais par l'hôte. Pendant le Leader : Échap annule, une touche sans commande ou un second Ctrl + Espace rend la frappe au terminal (ce dernier permet d’envoyer Ctrl + Espace à PSReadLine), l’expiration après 5 s l’annonce dans la barre de statut. Les événements en cours de composition IME (`isComposing`) passent toujours au terminal. `LEADER_HINTS` liste les séquences affichées par `LeaderHints`. |
| `src/components/` | Un composant par fichier : `AppShell` (monte `CommandPalette` et écoute Ctrl + P au niveau du document pour l'ouvrir quand le focus n'est pas dans un terminal ; à la fermeture le focus revient au pane actif, avant qu'un éventuel éditeur inline ne le prenne ; abonné aux seules valeurs qu'il affiche (`useShallow`), il lit les actions par `getState()` et définit hors du composant les gestionnaires transmis aux composants mémoïsés (dont les objets `WorkspacePanelActions` du panneau et `HeaderWorkspaceActions` du bandeau de l'en-tête), qui lisent les stores au moment de l'appel pour rester stables), `StatusBar` (pied de page : message de statut et « Non enregistré », seul abonné à ces deux valeurs), `SearchDialog` (boîte générique `role="dialog"` avec champ `combobox` et liste `listbox`, utilisée par `CommandPalette` et `ProjectPicker` : ↑ / ↓ déplacent la sélection en boucle et la gardent visible, Entrée exécute, Échap ou clic hors de la boîte ferme, la sélection revient au premier résultat à chaque frappe, liste vide affiche « Aucun résultat. » et Entrée n'exécute rien ; sélection marquée par le fond seulement ; une étoile à droite de chaque commande, ou Ctrl + Entrée sur la sélection, bascule le favori via `sessionStore.toggleFavorite`), `CommandPalette` (construit les entrées de la palette), `AgentStateIcon` (icône SVG par état : anneau animé en cours, disque ambre « ! » attente, cercle coché terminé, disque rouge « × » erreur, cercle « ? » inconnu ; couleurs par tokens), `AgentBadge` (pastille dans l'en-tête du pane : icône + « Claude Code · En attente », message de l'agent en infobulle), `AttentionToasts` (notifications flottantes en bas à gauche, montées par `AppShell` donc visibles même barre latérale repliée : une carte par pane en attente non acquitté avec « workspace › onglet », l'agent et son message sur trois lignes au plus ; « Rejoindre le terminal ↗ » sélectionne le pane et lui donne le focus, « × » ignore ; `agent.join` reçu après un clic sur une notification Windows fait la même chose que « Rejoindre » ; dans les deux cas la carte disparaît jusqu'au prochain changement d'état de ce pane ; aucune prise de focus ni changement de workspace automatique, la saisie n'est jamais interrompue), `CloseConfirmDialog` (boîte `role="alertdialog"` ouverte par `closeGuard` : titre ciblé (pane, onglet, workspace ou « Quitter Dock ? »), avertissement que les programmes seront arrêtés de force avec leur arbre de processus, une ligne par pane avec « workspace › onglet › dossier » et les noms de processus ; « Arrêter et fermer » a le focus, Entrée confirme, Échap, « Annuler » ou clic hors de la boîte annule sans rien arrêter), `SettingsDialog` (écran d'administration des quatre fichiers de réglages : demande `settings.get` à l'ouverture, formulaire par section (shells avec chemin par défaut en filigrane et badge Disponible / Introuvable, éditeur, persistance avec bornes, racine des projets), chemin de chaque fichier affiché, bouton « … » à côté de chaque chemin qui ouvre le sélecteur natif de l'hôte via `dialog.pick` (fichier pour les shells et l'éditeur, dossier pour la racine des projets ; la réponse `dialog.picked` arrive par `hostStore.pickedPath` et remplit le champ sans enregistrer), avertissements non bloquants en tête ; « Importer… » (`settings.import`) remplit le formulaire avec les réglages validés par l'hôte (`settings.imported` via `hostStore.importedPreferences`) et affiche un bandeau avec le fichier source, rien n'est écrit avant Enregistrer, qui remplace la configuration ; « Exporter… » (`settings.export`) écrit la configuration enregistrée dans un fichier JSON versionné ; les deux existent aussi dans la palette (« Exporter / Importer les préférences… ») ; section « Notifications » (notification Windows hors focus, case désactivée avec explication quand l'hôte la signale indisponible, clignotement de la barre des tâches, son parmi six choix ou « Fichier .wav de mon ordinateur… » avec champ de chemin et sélecteur natif (`dialog.pick` cible `sound`), bouton « Tester » qui envoie `attention.test` avec le brouillon sans enregistrer), section « Agents » qui affiche le chemin du script de hooks, l'état « Hooks installés / non installés » et un bouton « Installer les hooks » ou « Retirer les hooks » (`agents.installHooks` / `agents.removeHooks`, jamais sans clic) ; Enregistrer ou Ctrl + Entrée envoie `settings.save`, Échap ou Annuler ferme sans rien écrire ; à la réponse `saved`, `App` ferme la boîte, réarme la sauvegarde périodique, reconfigure le nombre de lignes des prochains terminaux et annonce le résultat dans la barre de statut), `ProjectPicker` (Leader puis F ou entrée « Ouvrir un projet » de la palette : demande `projects.list` à l'hôte, liste les dossiers réels de la racine configurée, Entrée crée un workspace nommé comme le dossier avec un premier terminal dedans ; racine absente affichée comme message, aucune modification du contexte courant), `Header` (bouton du panneau, identité et nom du workspace renommable en place ; panneau replié, le bandeau `HeaderWorkspaces` fourni par `AppShell` prend la place du nom et s'efface au profit de `LeaderHints` tant que le Leader est actif), `HeaderWorkspaces` (bandeau des seuls workspaces, dans l'ordre du panneau, visible uniquement panneau replié : un `HeaderWorkspaceItem` par workspace avec son nom tronqué à 200 px par `TruncatedName` et le même `WorkspaceStatus` que le panneau, pastilles cliquables comprises ; fond `green-soft` sur l'actif ; un clic sélectionne le workspace sur son dernier onglet actif et donne le focus à son pane actif, double-clic ou F2 le renomme en place (origine `RenameOrigin.Header`, comme Leader + W et la palette) ; aucun changement de workspace automatique ; `useStripWidths` mesure la place disponible et chaque élément, y compris ceux repliés, gardés hors flux et invisibles, et `visibleIndexes` (`workspaceStrip.ts`, qui porte aussi `HeaderWorkspaceActions`) garde ceux qui tiennent, dans l'ordre, en épinglant en dernière position l'élément en cours de renommage, sinon l'actif ; le reste passe dans `WorkspaceOverflow`), `WorkspaceOverflow` (bouton « +n » : nombre de workspaces repliés et pastilles cumulées de leurs attentes et erreurs, qui ouvre un `ActionMenu` les listant avec leur résumé d'état ; un choix sélectionne le workspace, Échap rend le focus au bouton), `LeaderHints` (rappel des séquences Leader dans l’en-tête tant que le Leader est actif), `WorkspaceTree` (panneau des workspaces, conteneur `@container` : en-tête « Workspaces » avec « Ouvrir un projet » (même action que Leader puis F) et « Nouveau workspace » ; reçoit de `AppShell` un objet d'actions stable `WorkspacePanelActions` (`workspacePanel.ts`, qui porte aussi `PanelMenuRequest`, `isMenuKey` et les classes partagées de la croix et de la ligne de dépôt) ; mémoïse la liste des noms de workspaces et tient l'état du menu contextuel ; chaque workspace est un `WorkspaceItem` mémoïsé qui ne reçoit l'onglet glissé, la cible de dépôt et l'onglet en renommage que s'ils le concernent. Un seul repère « vous êtes ici » : l'onglet actif (`green-deep` sur `green-soft`, sans gras), ou son workspace quand il est replié ; survol en `panel`. Ligne de workspace de 30 px : chevron (seul geste qui replie), nom en semibold, résumé `WorkspaceStatus`, croix au survol ou au focus ; un clic sur la ligne sélectionne, double-clic ou F2 renomme, clic droit ou Maj + F10 ouvre `WorkspaceContextMenu`. Onglets en `WorkspaceTabRow` de 28 px sous un guide de 1 px avec coude : tête de 14 px réservée même vide pour aligner les noms (icône de l'état dominant, sinon `TabLayoutGlyph`), nom en `ink-soft`, étiquette de shell en mono (pwsh, cmd, bash) masquée sous 260 px de panneau, croix au survol ; infobulle avec nom, shell, nombre de panes et agents ; clic active l'onglet, clic sur l'icône d'état rejoint le pane concerné, double-clic ou F2 renomme dans le panneau, clic molette ferme ; fermer un workspace ferme ses onglets un par un, les cinq derniers restant restaurables), `WorkspaceStatus` (résumé d'attention d'un workspace : une pastille par état qui demande une action (attente, erreur) avec le nombre de panes, cliquable pour rejoindre le pane suivant dans cet état, puis un seul glyphe d'activité (en cours, sinon terminé) ; l'état inconnu n'apparaît que dans l'infobulle, qui donne la répartition complète ; sans `onJoin`, les pastilles ne sont pas cliquables, comme dans le bouton « +n » et son menu), `TruncatedName` (nom sur une ligne : pour un nom de dossier sans espace, le préfixe partagé avec un voisin et terminé par `-`, `_` ou `.`, comme le `dock-terminal-` posé par `wtr`, se replie en premier pour garder la partie qui distingue ; un nom avec des espaces est tronqué en fin), `TabLayoutGlyph` (miniature SVG 14 × 11 de l'arbre de splits d'un onglet divisé, rien pour un seul pane), `FloatingMenu` (`ActionMenu` en position fixe au point demandé, gardé dans la fenêtre), `WorkspaceContextMenu` (menu contextuel du panneau bâti sur `FloatingMenu`, placé au pointeur ou sous la ligne au clavier : Renommer, Nouvel onglet PowerShell, Replier les autres, Fermer le workspace ; Renommer, Fermer l'onglet ; le focus revient à la ligne quand il a été ouvert au clavier), `Icon` (icônes SVG 12 px du panneau, noms dans `iconName.ts`), `TabBar` (même icône d'état dominant devant le nom de l'onglet ; bouton à droite qui affiche ou masque le panneau de droite de l'onglet actif sur sa dernière vue (`aria-pressed`) ; n'affiche l'éditeur inline que pour un renommage d'origine `RenameOrigin.TabBar`, celle par défaut du double-clic et de la palette), `SplitView` (récursif : chaque branche reçoit son chemin `SplitPath` depuis la racine, dimensionne ses enfants par `flex` selon `ratio` et intercale un `SplitResizer`), `SplitResizer` (séparateur `role="separator"` entre deux sous-zones : glisser au pointeur ou flèches au clavier par pas de 5 %, ratio borné 15–85 % par le modèle, persisté sans toucher aux processus), `PaneView` (mémoïsé : ne se redessine que si son pane, son état actif ou ses propres états changent ; en-tête avec le chemin ciblé et des boutons compacts : copier le chemin, ouvrir dans l'éditeur, ouvrir dans l'explorateur, copier la branche Git (désactivé et infobulle « Aucun dépôt Git » / « HEAD détachée » sans branche ; le contexte est relu à chaque changement de dossier), puis split côte à côte, split haut/bas et fermeture ; les actions de dossier existent aussi dans la palette pour le pane actif ; porte `data-pane-id` pour la navigation spatiale ; superpose `PaneOverlay` quand `paneStore` signale un échec, une fin de processus ou un dossier disparu), `PaneOverlay` (message local au pane : « Relancer » et « Choisir un shell » après échec ou fin, « Relancer dans le dossier de repli » et « Ignorer » quand le dossier n'existe plus, toujours « Fermer le pane » ; aucun repli silencieux), `PaneOverlay` (message local au pane avec « Relancer », « Choisir un shell » via `ShellMenu` et « Fermer le pane » ; aucun repli silencieux), `paneNavigation.ts` (enum `Direction` et `paneInDirection` : choisit le pane dont le rectangle DOM est le plus proche dans la direction demandée, aucun changement s’il n’y en a pas), `EmptyState` (aucun workspace : création ou réouverture du dernier onglet fermé), `InlineNameEditor` (renommage inline des workspaces et des onglets, ce dernier par double-clic dans `TabBar` : Entrée ou perte de focus valide, Échap annule ; le store ignore un nom vide et un onglet renommé passe en `manual`, ce qui fige son nom face aux changements de dossier), `Tooltip` (infobulle unique montée par `AppShell` : tout élément portant `data-tip` l'affiche après 250 ms au survol ou au focus clavier, en position fixe pour ne jamais être tronquée, jamais tant qu'un bouton de la souris est enfoncé (glisser, redimensionner) ; ne pas utiliser `title`), `SidebarResizer` (séparateur `role="separator"` d'un panneau latéral : glisser à la souris ou flèches gauche/droite au clavier, bornes et libellé fournis, `reversed` pour le panneau de droite qui s'élargit vers la gauche ; largeur bornée par le store), `RightPanel` (panneau à droite de la zone de travail, affiché quand l'onglet actif a `explorer` : onglets « Fichiers » et « Git » (`role="tablist"`, fond discret sur la vue affichée) et croix de fermeture, puis `FileExplorer` ou `GitPanel` selon `panel`), `FileExplorer` (vue Fichiers du panneau de droite : en-tête avec le nom du dossier du pane actif (chemin en infobulle), « Nouveau fichier », « Nouveau dossier » et « Actualiser » ; racine = dossier du pane actif, donc suit les `cd` et les changements de pane ; envoie la liste des dossiers à surveiller à chaque changement et une liste vide au démontage ; « Dossier vide. » quand il n'y a rien), `FileTree` (`role="tree"` à focus itinérant : ↑ / ↓ / Début / Fin déplacent la sélection, → déplie puis descend au premier enfant, ← replie puis remonte au parent, un clic déplie ou replie un dossier, double-clic ouvre un fichier dans l'éditeur, Entrée fait l'un ou l'autre, F2 renomme, Suppr demande la suppression, Maj + F10 ou touche Menu ouvre le menu, Échap rend le focus au terminal, Ctrl + Maj + E ferme ; saisie en place du nom d'un nouvel élément en tête du dossier cible ; clic droit sur le fond : actions du dossier racine), `FileTreeRow` (mémoïsé : ligne de 22 px indentée par niveau, chevron des dossiers, icône dossier ou fichier, nom tronqué ou `InlineNameEditor` pendant un renommage ; gestionnaires stables `FileTreeHandlers` de `fileTreeHandlers.ts`), `FileContextMenu` (`FloatingMenu` : ouvrir dans l'éditeur ou ouvrir un terminal ici (nouvel onglet dans ce dossier), nouveau fichier, nouveau dossier, renommer, supprimer, copier le chemin ; sur le fond : création, terminal, chemin, actualiser), `DeleteConfirmDialog` (boîte `role="alertdialog"` : « Supprimer « nom » ? », fichier ou dossier avec tout son contenu placé dans la corbeille de Windows, chemin complet ; « Placer dans la corbeille » a le focus, Entrée confirme, Échap, « Annuler » ou clic hors de la boîte annule et rend le focus à l'arbre), `GitPanel` (vue Git du panneau de droite, conteneur `@container` : suit le dépôt du dossier du pane actif (`git.watch` à chaque changement, arrêt au démontage, qui remet aussi la sélection sur la ligne WIP) ; « Lecture du dépôt Git… » puis « Aucun dépôt Git » hors dépôt ; `GitHeader`, `GitBanners`, `GitPromptBar`, puis le détail de la ligne choisie dans le graphe, sans onglets internes : ligne WIP → titre « Modifications » et `GitChangesView`, commit ou stash → `GitCommitDetail` ; barre de progression pendant une opération ; Échap ferme le volet, puis la saisie en place, puis donne le focus au graphe s'il est affiché, sinon au terminal, Ctrl + Maj + G ferme, Ctrl + Maj + E passe aux fichiers), `GitHeader` (nom du dépôt, branche ou « HEAD détachée », avance / retard sur l'amont ou « non publiée », bouton « Graphe » (`aria-pressed`) qui affiche ou masque le graphe, puis `GitToolButton` Fetch, Pull, Push (qui publie une branche sans amont), Annuler (infobulle : opération annulable, ou raison du refus) et Actualiser, libellés masqués sous 340 px), `GitToolButton` (bouton icône + libellé, `aria-disabled` pour garder l'infobulle, `aria-pressed` pour une bascule), `GitBanners` (opération en cours avec le nombre de conflits, « Terminer » actif une fois tout résolu et « Abandonner » ; push refusé avec « Pull » et « Forcer le push… », affiché seulement tant que l'hôte autorise le forçage ; échec avec la sortie de git dépliée, hooks compris), `GitPromptBar` (saisie en place d'un nom de branche avec « Checkout après création », d'un renommage, d'un tag ou d'un message de stash : Entrée valide, Échap annule), `GitChangesView` (conflits, Staged et Unstaged avec actions de groupe : unstage de tout, tout abandonner, stage de tout ; focus itinérant : ↑ / ↓ / Début / Fin, Entrée ouvre le diff ou ouvre un conflit dans l'éditeur, Espace fait le stage ou l'unstage du fichier, ou le marque résolu, Suppr abandonne ; « … et N autres fichiers » au-delà de 1 000 ; `GitCommitBox` en bas), `GitGroupHeader`, `GitFileRow` (mémoïsé : lettre de statut colorée avec son libellé en infobulle, nom, dossier, actions au survol ou au focus : ouvrir dans l'éditeur, abandonner, stage, unstage, marquer résolu), `GitCommitBox` (message, « Amend du dernier commit » qui reprend le dernier message, « Commit » et « Commit et push », Ctrl + Entrée ; boutons désactivés avec la raison en infobulle), `GitCommitDetail` (détail d'un commit ou d'un stash choisi dans le graphe : nature et SHA court, croix « Revenir aux modifications » ; pour un stash, boutons Appliquer, Appliquer et supprimer, Supprimer ; puis `GitCommitSummary`), `GitCommitSummary` (sujet, corps du message, SHA copiable, auteur, date, parents d'un merge ou commit de départ d'un stash, liste des fichiers à focus itinérant : ↑ / ↓ / Début / Fin, clic ou Entrée ouvre le diff du fichier dans le volet), `GitGraphView` (graphe affiché par-dessus les terminaux de l'onglet, sous la barre d'onglets, tant que la vue Git est ouverte avec `graphOpen` et qu'un dépôt est lu : `GitGraphToolbar`, colonne des branches `GitRefsSidebar` repliable et redimensionnable par `SidebarResizer`, `GitGraphTable`, `GitDragGhost` et le menu contextuel en cours ; Échap ferme le graphe et rend le focus au terminal, Ctrl + Maj + G ferme la vue, Ctrl + Maj + E passe aux fichiers), `GitGraphToolbar` (bascule de la colonne des branches, nom du dépôt, nombre de commits chargés, portée « Toutes » ou « Courante », croix de fermeture), `GitRefsSidebar` (sections repliables « Locales » (avance / retard), « Distantes » par dépôt distant, « Tags », « Stash » avec création de branche ou de tag sur HEAD et « Stash des modifications » ; un clic amène au commit dans le graphe (`revealCommit`), double-clic ou Entrée fait le checkout d'une branche ; les branches se glissent sur la branche courante ; menus contextuels de `gitMenus.ts`), `GitSection`, `GitRefRow` (ligne de la colonne des branches, source et cible de glisser-déposer quand elle porte une branche), `GitGraphTable` (liste virtualisée de lignes de 28 px avec `useVirtualRows` et en-tête collant `GitGraphHeader` : ligne WIP puis commits et stash, page suivante demandée en approchant de la fin, défilement vers le commit demandé par `reveal` ; `aria-activedescendant` : ↑ / ↓ / Page / Début / Fin changent la ligne choisie et le détail du panneau suit, Entrée donne le focus au panneau, Maj + F10 ou clic droit ouvre le menu de la ligne ; prend le focus à son affichage quand l'ouverture de la vue l'a demandé), `GitGraphHeader` (en-têtes Branche / Tag, Graphe, Message, Auteur, Date ; `GitColumnResizer` sur les colonnes à largeur fixe ; clic droit ou bouton de colonnes pour masquer Auteur, Date ou la colonne des branches), `GitColumnResizer` (séparateur `role="separator"` d'une colonne : glisser ou flèches par pas de 10 px, largeur bornée et enregistrée dans la session), `GitGraphRow` (mémoïsé : `GitRefLabels`, `GitGraphCell`, liseré de la couleur de la voie, sujet (gras sur HEAD, atténué pour un merge ou un stash), auteur, date `jj/mm/aaaa hh:mm` avec la date complète et relative en infobulle), `GitWorkingTreeRow` (ligne « // WIP » : nœud pointillé, opération en cours, nombre de fichiers en conflit, ajoutés, modifiés et supprimés, ou « Aucune modification »), `GitGraphCell` (SVG des voies : segments droits ou courbes, en pointillés vers HEAD ou vers le départ d'un stash, trait qui relie l'étiquette au nœud ; nœud aux initiales de l'auteur, pastille pour un merge, carré pointillé pour un stash, cercle pointillé pour la ligne WIP ; voies au-delà de la largeur de la colonne masquées), `GitRefLabels` (première étiquette puis « +n » avec les autres en infobulle, et trait vers le nœud), `GitRefPill` (étiquette teintée de la couleur de la voie : coche sur la branche courante, icône de branche locale, distante (nuage) ou de tag ; double-clic fait le checkout, clic droit ouvre son menu, une branche se glisse ; entourée quand elle est une cible de dépôt), `GitDragGhost` (étiquette qui suit le pointeur pendant un glisser, avec la cible ou l'indication de dépôt), `GitContextMenu` (`FloatingMenu` qui se ferme avant d'exécuter l'entrée), `GitDiffDrawer` (volet large accolé au bord droit de la zone des terminaux, par-dessus le graphe ou les terminaux, `min(920 px, largeur disponible)` : contexte (Unstaged, Staged, non suivi, commit ou stash), chemin du fichier, actions d'un fichier de travail, Échap ou croix le ferme et rend le focus au panneau Git), `GitDiffView` (diff virtualisé en lignes de 18 px : notes, en-têtes de blocs, anciens et nouveaux numéros, lignes ajoutées et retirées colorées, défilement horizontal, tabulations sur 4 colonnes), `GitConfirmDialog` (boîte `role="alertdialog"` des opérations destructrices : reset hard, branche sans merge ou distante, abandon de modifications, suppression d'un stash, abandon d'une opération en cours, push forcé ; le bouton de confirmation a le focus, Entrée confirme, Échap, « Annuler » ou clic hors de la boîte annule), `useVirtualRows.ts` (plage de lignes visibles d'une liste à hauteur fixe, mesurée au défilement et au redimensionnement), `gitGraphStyles.ts` (hauteurs de ligne et d'en-tête, classes des huit teintes de voie pour traits, pastilles, liserés et étiquettes, `GitNodeKind`, `ResizerSide`, gestionnaires `GitGraphRowHandlers`), `rightPanelStyles.ts` (classes partagées du panneau de droite), `tabDrag.ts` (cible de dépôt mise à jour seulement quand elle change ; glisser-déposer des onglets par événements pointeur et `elementFromPoint` sur les attributs `data-drop-*`, car le drag & drop HTML5 n'est pas relayé par la WebView2 ; état transitoire dans `uiStore` ; `TabBar` et `WorkspaceTree` sont sources et cibles : déposer sur un onglet insère avant lui, sur un workspace ou la barre ajoute en fin ; un workspace replié survolé 600 ms pendant un glisser s'ouvre (attribut `data-spring-workspace`, `springWorkspaceIds` dans `uiStore`) sans toucher à son état déplié mémorisé et se referme à la fin du glisser ; les instances xterm.js suivent leurs panes sans être recréées), `ActionMenu` (menu générique `role="menu"` : flèches / Début / Fin, Entrée, Échap, Tab ou clic extérieur ferment, entrées désactivables, en-tête optionnel, détail optionnel à droite de chaque entrée ; largeur ajustée au contenu jusqu'à 360 px, libellé tronqué au-delà), `ShellMenu` (`ActionMenu` des shells, ouvert par clic droit, Maj + F10 ou touche Menu sur le « + » de la barre d’onglets ; liste les shells disponibles annoncés par l’hôte, flèches / Début / Fin, Entrée, Échap, Tab ou clic extérieur). |
| `src/index.css` | Import Tailwind, police de symboles Nerd Font embarquée, tokens de la direction Dock (`--color-dock-*`, `--font-mono`), dont les cinq couleurs saturées d'état d'agent `--color-dock-status-*` réservées aux icônes et pastilles d'agents, les fonds et encres des lignes ajoutées et retirées d'un diff `--color-dock-diff-*` et les huit teintes désaturées des voies du graphe Git `--color-dock-lane-0` à `7`, contour `:focus-visible` global en `dock-focus` pour un focus clavier visible partout. |

## Flux

1. `App` envoie `app.ready` ; l'hôte répond `app.hello` avec la session persistée (ou initiale), les shells disponibles, le texte des panes de la session précédente et les réglages de persistance. Le texte est injecté dans chaque pane avant le premier attachement ; un message `recovery` s'affiche en avertissement quand un fichier a été mis en quarantaine.
2. `sessionStore.load` charge la session ; `SplitView` rend l'arbre de l'onglet actif ; chaque `TerminalPane` demande `terminal.create` à son premier attachement.
3. Toute mutation du store produit une nouvelle session par partage structurel (Immer : objets non modifiés conservés, session produite figée, jamais de mutation en place) ; `App` la sauvegarde après 500 ms de calme via `session.save` et détruit les terminaux dont le pane n'existe plus (le texte est alors renvoyé pour purger `text.json`). Le texte des terminaux part indépendamment toutes les 30 s (seuls les panes modifiés ; l'historique déjà sérialisé est réutilisé, le reste l'est par tranches pendant les temps morts) et à la fermeture ; un échec d'écriture affiche « Non enregistré » dans le pied de page jusqu'à la prochaine écriture réussie.
4. `terminal.cwd` (émis à chaque prompt) met à jour `pane.path` et efface un éventuel état local du pane ; quand ni le dossier ni le nom automatique de l'onglet ne changent, la session n'est pas modifiée : aucun rendu ni aucune sauvegarde ; le nom automatique de l'onglet, le contexte Git et les actions de dossier suivent ce chemin, ce qui couvre `wtr`. `terminal.pathMissing` (après `rmwt` ou au démarrage) marque le pane comme dossier disparu sans le fermer. `terminal.output` va directement à l'instance xterm.js sans passer par React.

## Règles

- Le modèle et le pont ne connaissent pas React. Les composants ne contiennent pas de logique métier : ils appellent les actions du store.
- Les handlers sont des fonctions nommées (`handleSelect`, `handleClose`), pas de lambdas inline avec logique dans le JSX.
- Enums TypeScript plutôt que types union pour les valeurs fermées (`SplitAxis`, `StatusLevel`, `Renderer`). `erasableSyntaxOnly` est désactivé pour cela.
- Couleurs via les tokens `dock-*` de `index.css`, jamais en dur dans les composants. Le thème xterm.js est centralisé dans `terminalRegistry`.
- Textes visibles en français. Les termes Git restent en anglais et invariables, employés comme des noms : Push, Pull, Fetch, Stash, Stage / Unstage (Staged / Unstaged), Merge, Rebase, Amend, Checkout, Cherry-pick (« Push vers origin/main », « Faites un pull »).
- Fichiers de moins de 400 lignes, un composant exporté par fichier.

## Commandes

```
cd web
pnpm install
pnpm dev        # serveur Vite sur http://localhost:5173, à combiner avec DOCK_WEB_DEV_URL côté hôte
pnpm build      # tsc -b puis vite build vers web/dist
pnpm lint       # oxlint
```

Aucun test web pour l'instant (décision du 21 septembre 2026) ; le modèle est validé côté hôte par `SessionValidator` et ses tests xUnit.
